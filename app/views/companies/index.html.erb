<%= render 'company_activity'%>
<div id="main-content" class='<%= "#{ 'side-show' if side_nav_opened?}"%>'>
  <!--- main content --->
  <div class="flex-wrapper">
    <!--- breadcrum Bar --->
    <div class="flex-item breadcrum-bar">
      <div class="card-white-panel ">
        <h3 class="heading_with_action">
          <span><%= link_to 'Companies', companies_url %></span>
          <%= link_to "/ #{@status.humanize}", 'javascript:;', class: 'active' %>
          <%= link_to raw('<i class="material-icons">add</i>'), new_company_path, remote: true, class: 'pull-right', title: 'Add' %>
        </h3>
      </div>
    </div>
  </div>

  <div class="invoice-box mCustomScrollbar clearfix">
    <%= form_tag "companies/bulk_actions", {method: :get, id: "bulk_action_form"} do -%>
        <div class="search_result_container"><%= render 'companies_container' %></div>
        <%= render partial: 'bulk_actions'%>
    <% end %>
  </div>

</div>