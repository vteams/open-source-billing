<div id="sort_direction" style="display: none;"><%= params[:direction] %></div>
<%= hidden_field_tag :direction, params[:direction] %>
<%= hidden_field_tag :sort, params[:sort] %>

<div class="company">
  <div class="content_top">
    <div class="primary_heading">
      <h1>
        <%= t('views.companies.companies') %>
        <span>&nbsp;/&nbsp;<%= t('views.common.active') %></span>
      </h1>
    </div>
    <div class="primary_btn">
      <%= link_to (content_tag(:span, t("views.companies.new"))),
                  new_company_path,
                  :class => 'btn_large' %>
    </div>
  </div>
  <!--content_top-->
  <%= form_tag "companies/bulk_actions", {method: :get, :remote => true} do -%>
      <div class="content_middle">
        <div class="grid_control">
          <div class="grid_control_left">
            <ul value='company' id="bulk_actions">
              <%= render "bulk_actions" %>
            </ul>

          </div>
          <div class="grid_control_right" id="active_links">
            <%= render "filter_links" %>
          </div>
        </div>
        <div class="grid_table">
          <table cellpadding="0" cellspacing="0" class="table table_listing company_listing">
            <colgroup>
              <!--<col class="one"/>-->
              <col class="one"/>
              <col class="two"/>
              <col class="three"/>
              <col class="four"/>
              <col class="five"/>
              <col class="six"/>
            </colgroup>
            <thead id = 'company_head'>
            <%= render 'companies/companies_header'%>
            </thead>
            <tbody id="company_body">
                <%= render 'company' %>
                <tfoot id="paging_links">
                    <%= render "application_partials/paging" %>
                </tfoot>
            </tbody>
          </table>
        </div>
        <!--grid_table-->
      </div>
      <!--content_middle-->
      <div class="content_bottom">
        <%= link_to (content_tag(:span, t("views.companies.new"))),
                    new_company_path,
                    :class => 'btn_large' %>
      </div>
      <!--content_bottom-->
  <% end %>
</div>
