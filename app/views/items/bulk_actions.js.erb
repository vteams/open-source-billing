swal({
    icon: "success",
    title: 'Success',
    buttons: false,
    timer: 3000,
    text: "<%= t('views.items.bulk_action_msg', action: @action) %>"}).then(function(){
    window.location = '<%= items_path %>'
});