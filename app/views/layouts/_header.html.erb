<!--- header --->
<header id="header">
  <div class="row">
    <!--- header logo--->
    <div class="col s12 m5 l4">
      <a class="btn-menu" href="javascript:;"><i class="material-icons">menu</i></a>
      <%= link_to raw('Open<span>Source</span>Billing'), dashboard_path, class: 'logo'%>
    </div>
    <!--- header company name--->
    <div class="col s9 m5 l4">
      <div class="header-center">
        <div class="hdr-select-holder">
          <select class="company_select">
            <option value="" disabled selected><%= get_company_name%></option>
            <% filter_select_by_companies.each do |company| %>
                <option value="<%= company.id %>" data-company-id="<%= company.id %>" data-controller="<%= params[:controller]%>" data-action="<%= params[:action]%>"><%= company.company_name %></option>
            <% end %>
            <optgroup label="">
              <option value="-1"><%= t('views.companies.new') %></option>
            </optgroup>
          </select>
          <%= link_to '', new_company_path(remote: 1, reload: 1), remote: true, class: 'hidden', id: 'new_company_remote_link' %>
          <% if current_user and current_user.settings.currency.present? and current_user.settings.currency.eql?("On") %>
              <select class="currency currency_select" data-user-id="<%= current_user.id %>">
                <option value="" disabled selected><%= current_user.settings.default_currency %></option>
                <% currencies.each do |currency| %>
                    <option value="<%= currency.id%> "><%= currency.unit %></option>
                <% end %>
              </select>
         <% end %>
        </div>
      </div>

    </div>
    <!--- header search--->
    <div class="col s3 m2 l4  header-right">
      <div class="right">
        <%= link_to raw('<i class="material-icons">search</i>'), 'javascript:;', class:'btn-search',
                    title: t('header.links.search') if OSB::CONFIG::ENABLE_SEARCH and search_available_in?(controller_name)%>
        <%= link_to raw('<i class="material-icons">help_outline</i>'), help_index_path, class: 'help-search',
                    title: t('views.help.help'), remote: true %>
      </div>
      <!--- header search holder--->
      <div class="search-holder">
        <a href="#" title="<%= t('views.common.back') %>" class="btn-back"><i class="zmdi zmdi-arrow-left"></i><%= t('views.common.back') %></a>
        <div class="container">
          <div class="checkboxinfo row">
            <div class="left">
              <div class="chk-text"></div>
            </div>
            <%= render 'layouts/bulk_action_links'%>
          </div>
        </div>
        <!--<form action="#">-->
          <!--<i class="zmdi zmdi-search"></i>-->
          <!--<input type="text" class="form-control" placeholder="" />-->
        <!--</form>-->
        <% if OSB::CONFIG::ENABLE_SEARCH and search_available_in?(controller_name)%>
            <div class="search">
              <%= form_tag eval("#{controller_name}_path"), method: :get do %>
                  <i class="zmdi zmdi-search"></i>
                  <%= filter_box_for  %>
              <% end %>
            </div>
        <% end %>
      </div>
    </div>
  </div>
</header>