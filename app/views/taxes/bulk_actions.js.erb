swal({
    icon: "success",
    title: 'Success',
    buttons: false,
    timer: 3000,
    text: "<%= t('views.taxes.bulk_action_msg', action: @action) %>"}).then(function(){
    window.location = '<%= taxes_path %>'
});