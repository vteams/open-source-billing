<% if @taxis.present? %>
swal({
    icon: "success",
    title: 'Success',
    buttons: false,
    timer: 3000,
    text: "<%= t('views.taxes.created_msg') %>"}).then(function(){
    window.location = '<%= taxes_path %>'
});
<%end%>