<% if @tax_updated.eql?(true) %>
  $('.modal').modal('close')
  swal({
      icon: "success",
      title: 'Success',
      buttons: false,
      timer: 3000,
      text: "<%= t('views.taxes.updated_msg') %>"}).then(function(){
      window.location = '<%= taxes_path %>'
});
<%end%>