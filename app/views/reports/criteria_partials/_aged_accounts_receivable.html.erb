<%= form_tag "/reports/data/#{@report.report_name}", :method => :get, :remote => true, :id => "reports_form" do %>
    <div class="row">
      <div class="input-field col s6 m6 xl2  datepicker">
        <input name="criteria[to_date]" type="text" id="to_date" readonly value="<%= Date.today.strftime(get_date_format) %>"/>
        <i class="material-icons">date_range</i>
        <label><%= t('views.reports.period_ending') %></label>
      </div>
      <div class="input-field col s6 m6 xl3">
        <%= collection_select "criteria", "client_id", [Client.new({:id => 0, :organization_name =>t("views.common.all")})] + get_report_clients, :id, :organization_name, {:prompt => "All"} %>
        <label><%= t('views.clients.clients') %></label>
      </div>
      <div class="col s12 m1">
        <%= submit_tag t('views.common.go'), :class => "report_update hidden" %>
        <a href="#" class="icon-done" onclick="$('.report_update').click();"><i class="material-icons">done</i></a>
      </div>
    </div>
<% end %>