<%= form_tag "/reports/data/#{@report.report_name}", :method => :get, :remote => true do %>
  <div class="row">
    <div class="input-field col s12 m6 xl3">
      <%= select_tag "criteria[quarter]", options_for_select(QUARTER_MONTHS)%>
      <label><%= t('views.common.quarter') %> </label>
    </div>
    <div class="input-field col s12 m6 xl3">
      <%= select_tag "criteria[year]", options_for_select(((Date.today.year - 1)..(Date.today.year + 5)).each { |y| [y, y] }, Date.today.year) %>
      <label><%= t('views.common.year') %></label>
    </div>
    <div class="input-field col s12 m6 xl3">
      <%= collection_select "criteria", "client_id", [Client.new({:id => 0, :organization_name => t('views.common.all')})] + get_report_clients, :id, :organization_name, {:prompt => t('views.common.all')} %>
      <label><%= t('header.links.clients.clients') %></label>
    </div>
    <div class="col s12 m1">
      <%= submit_tag t('views.common.go'), :class => "report_update hidden" %>
      <a href="#" class="icon-done" onclick="$('.report_update').click();"><i class="material-icons">done</i></a>
    </div>
  </div>
<% end %>
