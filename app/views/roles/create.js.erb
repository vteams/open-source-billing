<% if @role.errors.present? %>
  swal({
     icon: "error",
     title: "oops..!",
     buttons: false,
     timer: 3000,
      text: "<%= @role.errors.full_messages.join('.') %>",
      html: true
  });
<% else %>
  swal({
     icon: "success",
     title: "Success",
     buttons: false,
     timer: 3000,
    text: "Role created successfully"}).then(function(){
//    window.location = '<%#= settings_path %>'

  });
$('#roles_listing').html('<%= escape_javascript render(template: 'roles/roles_settings',
   layout: false, format: :html) %>');
initRoleListingEvents();
<% end %>