<%= render 'invoice_activity'%>
<div id="main-content" class='<%= "#{ 'side-show' if side_nav_opened?}"%> scrollContainer'>
  <div class="flex-wrapper">
    <div class="flex-item col-sm-12 breadcrum-bar">
      <div class="card-white-panel">
        <h3>
          <span><%= link_to t('header.links.invoices.invoices'), invoices_url %> </span>
          <%= link_to "/ #{t('views.common.active')}", invoices_url %>
        </h3>
      </div>
    </div>
  </div>

  <div class="invoice-box-holder">
    <%= render partial: 'form', locals: {invoice: @invoice} %>
  </div>
</div>
