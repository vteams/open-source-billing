swal({
    icon: "success",
    title: 'Success',
    buttons: false,
    timer: 3000,
    text: "<%= t('views.invoices.bulk_action_msg', action: @action) %>"}).then(function(){
    window.location = '<%= invoices_path %>'
});