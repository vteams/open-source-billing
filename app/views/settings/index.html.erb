<%= render 'setting_activity' %>

<div id="main-content" class='<%= "#{ 'side-show' if side_nav_opened?}"%> mCustomScrollbar' >
  <!--- main content --->
  <!--- breadcrum Bar --->
  <div class="breadcrum-bar card-white-panel clearfix">
    <h3> <%= t('views.settings.settings') %> </h3>
  </div>
  <%= render 'basic_setting'%>

  <div class="page-box">
    <%= render 'email_templates'%>
  </div>
</div>
<script>
   window.user_settings_link = "<%= settings_listing_sub_users_path %>";
   window.comapnies_settings_link = "<%= settings_listing_companies_path %>";
   window.roles_settings_link = "<%= roles_settings_roles_path %>";
  loadUsersActivitiesSection();
  loadCompaniesActivitiesSection();
  loadRolesActivitiesSection();
  // initRoleListingEvents();
</script>