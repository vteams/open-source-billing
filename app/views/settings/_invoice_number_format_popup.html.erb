<div id="invoice_number_formatModal" class="modal editableModal templateModal">
  <div class="modal-content">
    <div class="modal-header clearfix text-content">
      <div class="modal-action-btn right">
        <a href="#" onclick="$('.submit_invoice_format').click();" class="icon-done" title="<%= t('views.common.save') %>">
          <i class="material-icons">done</i>
        </a>
        <a href="javascript:void(0);" title="<%= t('views.common.cancel') %>" class="modal-action modal-close waves-effect">
          <i class="material-icons close">close</i>
        </a>
      </div>
    </div>
    <%= simple_form_for :settings, url: '/settings', remote: true do |f| %>
      <div class="modal-body">
        <div class="row">
          <%= f.input :invoice_number_format, input_html: {name: 'invoice_number_format', id: 'invoice_number_format', value: current_user.settings.invoice_number_format  }, label: t('Invoice Number Format'),
                      wrapper_html: { class: 'input-field col s12 m6 xl6' }, label_html: { class: 'active' }%>

          <%= f.submit '', class:  'btn_large submit_invoice_format hidden' %>
        </div>
        <div class="row">
          <aside class="col s12 m12 l3">
            <nav class="templatsidebar">
              <h5><%= t('views.email_templates.custom_fields') %>:</h5>
              <ul>
                <% CUSTOM_FIELDS['New Invoice'].each do |cf|%>
                  <li>
                    <strong><%= cf[:label] %> </strong>
                    <span><%= cf[:token] %> </span>
                  </li>
                <% end %>
              </ul>
            </nav>
          </aside>

        </div>
      </div>
    <% end %>
  </div>
</div>