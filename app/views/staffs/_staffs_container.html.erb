<% @staffs.group_by { |d| d.group_date }.each do |date, staffs| %>
    <p><span class="activity-date"><%= date %></span></p>
    <div class="flex-wrapper">
      <%= render partial: 'staff_card', collection: staffs, as: :staff %>
    </div>
<% end %>