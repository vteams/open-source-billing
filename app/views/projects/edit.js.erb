<% if params[:search].present? %>
    window.location = '<%= projects_url(search: params[:search]) %>'
<% else %>
    $('body').append("<%= escape_javascript render partial: 'project_form_popup', locals: {project: @project}  %>");

//    $('select').material_select();
    Project.change_project_task();
    Project.change_project_staff();
    Project.load_functions();
    $('.editProject .modal-content label').addClass('active');
    elem = $("#project_form_popup");
    Popup.initialize(elem);
    Popup.open(elem);
<% end %>