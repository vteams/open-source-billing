<% sort_class = params[:direction] == 'desc' ? 'sortdown' : 'sortup' %>
<tr>
  <th class="align_center not-sortable"><%= check_box_tag 'select_all' %></th>
  <th class="align_center  not-sortable"> <%= "Name" %></th>
  <th class="align_center  not-sortable><%= t('description') %></th>
  <th class="align_center  not-sortable %>"><%= sortable('rate', t('views.tasks.rate')) %></th>
  <th class="align_center not-sortable ">Action(s)</th>
</tr>