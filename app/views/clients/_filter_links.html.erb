<ul>
  <li class="grid_control_sep sep_right">
    <%= link_to "Active", "clients/filter_clients?status=active&per=#{params[:per]}", :remote => true, :class => "get_actives active" %>
    <span class="item_counts" id="actives">(<%= Client.unarchived.size %>)</span>
  </li>

  <li class="grid_control_sep sep_right">
    <%= link_to "Archived", "clients/filter_clients?status=archived&per=#{params[:per]}", :remote => true, :class => "get_archived" %>
    <span class="item_counts" id="archives">(<%= Client.archived.size %>)</span>
  </li>

  <li>
    <%= link_to "Deleted", "clients/filter_clients?status=deleted&per=#{params[:per]}", :remote => true, :class => "get_deleted" %>
    <span class="item_counts" id="deletes">(<%= Client.only_deleted.size %>)</span>
  </li>
</ul>
